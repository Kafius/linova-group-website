---
// Site chrome: navy + gold + film only — vertical accents never touch this.
// The nav's Book link participates in the one-CTA-per-viewport law: pages with
// a hero CTA mark it [data-hero-cta]; a tiny observer below hides the nav CTA
// while the hero CTA is on screen.
import { nav, site } from '../../data/site';
---

<header class="sticky top-0 z-40 bg-navy/90 backdrop-blur-sm">
  <div class="mx-auto flex max-w-[1200px] items-center justify-between gap-6 px-6 py-4">
    <a href="/" class="font-display text-2xl leading-none text-film" aria-label={site.name}>
      Linova<span class="text-gold">/</span>
    </a>

    <nav aria-label="Main">
      <ul class="hidden items-center gap-8 md:flex">
        {nav.links.map((link) => (
          <li>
            <a
              href={link.href}
              class="text-caption text-film-dim transition-colors hover:text-film"
            >
              {link.label}
            </a>
          </li>
        ))}
      </ul>
    </nav>

    <div class="flex items-center gap-3">
      <a
        href={nav.cta.href}
        data-nav-cta
        data-track="book_click"
        data-track-label="nav"
        class="hidden rounded-chip border border-gold px-4 py-2 text-caption text-film transition-colors hover:border-redline sm:inline-block"
      >
        {nav.cta.label}
      </a>
      <button
        type="button"
        data-nav-toggle
        aria-expanded="false"
        aria-controls="mobile-nav"
        class="flex h-10 w-10 items-center justify-center md:hidden"
        aria-label="Menu"
      >
        <svg width="20" height="14" viewBox="0 0 20 14" fill="none" aria-hidden="true">
          <path d="M0 1h20M0 7h20M0 13h20" stroke="currentColor" stroke-width="1.5" />
        </svg>
      </button>
    </div>
  </div>

  <div id="mobile-nav" hidden class="border-t border-grid/40 bg-navy px-6 py-6 md:hidden">
    <ul class="flex flex-col gap-5">
      {nav.links.map((link) => (
        <li>
          <a href={link.href} class="font-display text-heading text-film">{link.label}</a>
        </li>
      ))}
      <li>
        <a
          href={nav.cta.href}
          class="mt-2 inline-block rounded-chip border border-gold px-4 py-2 text-caption text-film"
        >
          {nav.cta.label}
        </a>
      </li>
    </ul>
  </div>
</header>

<script>
  // Mobile menu toggle
  const toggle = document.querySelector<HTMLButtonElement>('[data-nav-toggle]');
  const menu = document.getElementById('mobile-nav');
  toggle?.addEventListener('click', () => {
    const open = toggle.getAttribute('aria-expanded') === 'true';
    toggle.setAttribute('aria-expanded', String(!open));
    if (menu) menu.hidden = open;
  });

  // One CTA per viewport: the nav's Book link appears only after the page's
  // hero CTA (if any) has scrolled away.
  const navCta = document.querySelector<HTMLElement>('[data-nav-cta]');
  const heroCta = document.querySelector<HTMLElement>('[data-hero-cta]');
  if (navCta && heroCta && 'IntersectionObserver' in window) {
    const observer = new IntersectionObserver(([entry]) => {
      navCta.classList.toggle('sm:inline-block', !entry.isIntersecting);
      navCta.classList.toggle('hidden', entry.isIntersecting);
    });
    observer.observe(heroCta);
  } else if (navCta && !heroCta) {
    navCta.classList.remove('hidden');
    navCta.classList.add('sm:inline-block');
  }
</script>
